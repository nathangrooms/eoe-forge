/**
 * The pieces of the motion vocabulary that are components rather than hooks.
 *
 * The tokens themselves, the reduced-motion hook and the list primitives live
 * in `@/lib/motion`; the CSS half lives under MOTION in `src/index.css`.
 */

export { Changed, type ChangedProps } from './Changed';
export { RouteEnterMotion } from './RouteEnterMotion';
