/**
 * Moved. Tag information-content weighting now lives in the engine.
 *
 * `src/engine/knowledge/tag-signal.ts` is the only copy. See the note in
 * `tagger.ts` beside this file for why the old path still exists.
 */
export * from '../../engine/knowledge/tag-signal.ts';
