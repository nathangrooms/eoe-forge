/**
 * The date the wording of the terms and the privacy page last changed.
 *
 * One constant, read by both documents, so the two can never disagree about
 * when they were written. Change it in the same commit as the wording, and not
 * otherwise: a "last updated" that moves on its own is the exact stale-date
 * signal a careful reader looks for.
 */
export const LEGAL_UPDATED = '29 August 2026';
