/**
 * `/shopping` — the shopping list.
 *
 * The page body lives in `src/components/shopping/ShoppingListPage.tsx`, beside
 * the rest of the list feature, so the shopping list and the proxy list stay in
 * one folder rather than one of them drifting off into `pages/`.
 */
export { default } from '@/components/shopping/ShoppingListPage';
