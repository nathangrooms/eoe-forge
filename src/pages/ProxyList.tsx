/**
 * `/proxies` — the proxy list.
 *
 * The body lives in `src/components/shopping/ProxyListPage.tsx` because a proxy
 * list and a shopping list are the same primitive, and splitting them across
 * two folders is the first step towards splitting them into two systems.
 */
export { default } from '@/components/shopping/ProxyListPage';
